// Create request body
let requestBody = {
    MemshakUser: csvData.MemshakUser,
    // Internal structure, if needed
    obj2: [
        {
            MemshakUser: csvData.MemshakUser2,
            // Additional fields
        }
    ]
};